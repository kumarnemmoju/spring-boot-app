# Server port (optional - Tomcat will use its own port 8083)
server.port=8080

# Application context path
server.servlet.context-path=/spring-boot-app

# Spring Boot configuration
spring.application.name=spring-boot-app

# Disable default Tomcat banner
spring.main.banner-mode=off